package com.mta.location.util;

public class LocationCache {

//    private static final int EQUATOR_RADIUS = 6378;
//    private static final int POLAR_RADIUS = 6378;


//    public static double getDistance(Location location1, Location location2) {
//        double lonD = ((location2.lon - location1.lon) / 360) * (EQUATOR_RADIUS * 2 * Math.PI);
//        double latD = ((location2.lat - location1.lat) / 360) * (POLAR_RADIUS * 2 * Math.PI);
//        return Math.sqrt(Math.pow(lonD, 2)+ Math.pow(latD, 2));
//    }
}
